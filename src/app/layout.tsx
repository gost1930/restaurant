import type { Metadata } from "next";
import { Beiruti } from "next/font/google"; // get arabic font
import "./globals.css";
import { NavBar, Footer } from "@/components";

const beiruti = Beiruti({
  variable: "--font-beiruti",
  subsets: ["arabic"],
});
export const metadata: Metadata = {
  title: "Restaurant Web App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body dir="rtl" className={` ${beiruti.variable} antialiased max-w-screen min-h-screen`}>
        <div className="max-w-screen min-h-screen lg:mx-20 md:mx-10 mx-5 gap-y-4">
          {/* nav bar */}
          <NavBar />
          {children}
        </div>
        {/* footer */}
        <Footer />
      </body>
    </html>
  );
}
